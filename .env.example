# App: only the URL is public. Use your computer's LAN IP when testing on a phone.
EXPO_PUBLIC_ROUTES_API_URL=http://localhost:3001
# Server only: never use EXPO_PUBLIC_ for this key.
GOOGLE_MAPS_API_KEY=
APP_ORIGIN=http://localhost:8081
# For phone testing on the same trusted Wi-Fi, set HOST=0.0.0.0
HOST=127.0.0.1
PORT=3001
