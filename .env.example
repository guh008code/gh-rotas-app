# Shared key used directly by the app. Obtain it at https://account.heigit.org/
# This value is included in the compiled app and consumes the shared account quota.
# Keep the real value in .env.local (ignored by Git) and in the EAS build environment.
EXPO_PUBLIC_ORS_API_KEY=
